version: '3.8'

services:
  api:
    build: 
      context: .
      target: development
    environment:
      - NODE_ENV=development
      - DATABASE_URL=file:/data/db.sqlite
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run dev